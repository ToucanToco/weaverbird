version: 2

jobs:
  build:
    docker:
      - image: circleci/node:11
    steps:
      - checkout
      - run: yarn
      - run: yarn build-bundle
  test:
    docker:
      - image: circleci/node:11
    steps:
      - checkout
      - run: yarn
      - run: yarn test:unit

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
