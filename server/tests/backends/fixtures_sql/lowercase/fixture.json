{
  "step": {
    "name": "lowercase",
    "pipeline": [
      {
        "name": "lowercase",
        "column": "NAME"
      }
    ]
  },
  "input": {
    "schema": {
      "fields": [
        {
          "name": "NAME",
          "type": "string"
        },
        {
          "name": "AGE",
          "type": "integer"
        },
        {
          "name": "SCORE",
          "type": "integer"
        }
      ],
      "pandas_version": "0.20.0"
    },
    "data": [
      {
        "NAME": "FoO",
        "AGE": 42,
        "SCORE": 100
      }
    ]
  },
  "other_inputs": {
    "buzz": {
      "schema": {
        "fields": [
          {
            "name": "NAME",
            "type": "string"
          },
          {
            "name": "SCORE",
            "type": "integer"
          },
          {
            "name": "x",
            "type": "string"
          }
        ],
        "pandas_version": "0.20.0"
      },
      "data": [
        {
          "NAME": "plop",
          "SCORE": 666,
          "x": "y"
        }
      ]
    }
  },
  "expected": {
    "schema": {
      "fields": [
        {
          "name": "NAME",
          "type": "string"
        },
        {
          "name": "AGE",
          "type": "number"
        },
        {
          "name": "SCORE",
          "type": "integer"
        }
      ],
      "pandas_version": "0.20.0"
    },
    "data": [
      {
        "NAME": "foo",
        "AGE": 42.0,
        "SCORE": 100
      }
    ]
  },
  "other_expected": {
    "sql": {
      "query": "WITH SELECT_STEP_0 AS (SELECT * FROM lowercase_fixture), LOWERCASE_STEP_1 AS (SELECT AGE, SCORE, LOWER(NAME) AS NAME FROM SELECT_STEP_0) SELECT NAME, AGE, SCORE FROM LOWERCASE_STEP_1"
    },
    "snowflake": {
      "query": "WITH SELECT_STEP_0 AS (SELECT * FROM lowercase_fixture), LOWERCASE_STEP_1 AS (SELECT AGE, SCORE, LOWER(NAME) AS NAME FROM SELECT_STEP_0) SELECT NAME, AGE, SCORE FROM LOWERCASE_STEP_1"
    }
  }
}
